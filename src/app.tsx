import React from 'react';
import Nav from './nav';

const App: React.FC = () => {
  return (
    <div>
      <Nav />
      <div>Create React App Without CRA😊</div>
      <button>Click Me 😎</button>
    </div>
  );
};

export default App;
